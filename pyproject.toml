[project]
name = "unit-converter"
version = "0.1.0"
description = "A simple web app that can convert between different units of measurement"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.1",
    "flask-wtf>=1.2.2",
    "python-dotenv>=1.1.1",
]
